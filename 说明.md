files/etc/uci-defaults/99-custom.sh   改网络IP dns首次运行
shell/apk-custom-packages.sh    25.12.x安装改插件文件
shell/custom-packages.sh           24.10.x安装改配制插件
shell/switch_repository.sh  改构建插件地址

更新源
https://mirrors.vsean.net/openwrt/releases/25.12.2/targets/x86/64
https://mirrors.zju.edu.cn/immortalwrt//releases/25.12.2/packages/x86_64/luci/
https://mirrors.ustc.edu.cn/immortalwrt/releases/25.12.2/targets/x86/64/
https://dl.openwrt.ai/packages-25.12/x86_64/kiddin9/  第三方插件源

https://github.com/sirpdboy/netspeedtest/releases   luci-app-netspeedtest
https://github.com/zzsj0928/luci-app-pushbot/releases  luci-app-pushbot
https://github.com/liandu2024/Open-Box/releases  Open-Box 


cat << 'EOF' > /root/updata.sh
#!/bin/sh

# 这里填写你的脚本逻辑
echo "Starting update process..."

# 示例：更新软件包列表或执行更新
# opkg update

EOF

# 1. 停止并禁用后台正在运行的服务
/etc/init.d/pushbot stop 2>/dev/null || true
/etc/init.d/pushbot disable 2>/dev/null || true
/etc/init.d/open-box stop 2>/dev/null || true
/etc/init.d/open-box disable 2>/dev/null || true
open-box stop 2>/dev/null || true

# 2. 清理 luci-app-pushbot 相关文件
rm -f /etc/init.d/pushbot
rm -rf /etc/config/pushbot
rm -rf /usr/share/pushbot
rm -f /usr/share/luci/menu.d/luci-app-pushbot.json
rm -rf /usr/share/rpcd/acl.d/luci-app-pushbot.json
rm -rf /www/luci-static/resources/view/pushbot
rm -rf /usr/lib/lua/luci/controller/pushbot* 2>/dev/null || true
rm -rf /usr/lib/lua/luci/model/cbi/pushbot* 2>/dev/null || true
rm -rf /usr/lib/lua/luci/view/pushbot* 2>/dev/null || true

# 3. 清理 luci-app-netspeedtest 相关文件及组件
rm -rf /etc/config/netspeedtest
rm -f /usr/share/luci/menu.d/*netspeedtest*.json
rm -rf /usr/share/rpcd/acl.d/*netspeedtest*.json
rm -rf /www/luci-static/resources/view/netspeedtest
rm -rf /usr/lib/lua/luci/controller/*netspeed* 2>/dev/null || true
rm -f /usr/bin/speedtest /usr/bin/homebox /usr/bin/netspeedtest 2>/dev/null || true

# 4. 清理 Open-Box 面板及二进制核心
rm -rf /etc/open-box
rm -rf /usr/share/open-box
rm -f /usr/bin/open-box /usr/local/bin/open-box 2>/dev/null || true
rm -f /etc/init.d/open-box
rm -f /usr/share/luci/menu.d/*open-box*.json 2>/dev/null || true

# 5. 清理 LuCI 前端视图缓存并重启服务
rm -rf /tmp/luci-*
/etc/init.d/rpcd restart
/etc/init.d/uhttpd restart
